# Public API base URL (no trailing slash). Required for production (Railway, Vercel, etc.).
# Local dev defaults to http://localhost:8000 when unset.
NEXT_PUBLIC_API_URL=https://your-railway-backend.up.railway.app
